#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
npx --yes -p @commitlint/cli -p @commitlint/config-conventional \
  commitlint --config "${ROOT}/linters/commitlint.config.mjs" --edit "${1:-}"
