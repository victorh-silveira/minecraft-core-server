apiVersion: v1
kind: Service
metadata:
  name: mc-server-game
  namespace: mc-prod
  labels:
    app.kubernetes.io/name: mc-server
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: mc-server
  ports:
    - name: minecraft
      port: 25565
      targetPort: 25565
      protocol: TCP
